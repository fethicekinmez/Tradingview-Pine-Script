//@version=6
indicator("Bir Kez Label", overlay = true)

userInput = 55
txt = "Girilen Sayı: " + str.tostring(userInput)

var label myLabel = na

// Önceki label varsa sil
if not na(myLabel)
    label.delete(myLabel)

// Yeni label oluştur
myLabel := label.new(bar_index, close, text=txt, style=label.style_label_left, textcolor=color.white, size=size.normal, color=color.red)
